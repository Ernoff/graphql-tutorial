# import Link from "./generated/graphcool.graphql"

type Query {
  feed(filter: String, skip: Int, first: Int): [Link!]!
}

type Mutation {
  createDraft(title: String!, text: String): Link
  publish(id: ID!): Link
}
